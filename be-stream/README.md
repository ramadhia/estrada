# BE Stream

- create python virtual env (_optional_) and activate it

  ```bash
  python -m venv .venv

  source .venv/bin/activate
  ```

- start REST API server

  ```bash
   python main.py    
  ```
